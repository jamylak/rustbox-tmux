[package]
name = "rustbox-tmux"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
incremental = false

[profile.dev]
debug = 1
incremental = true
